[package]
name = "renew-protocol"
version = "0.1.0"
edition = "2021"
description = "Renew multichain settlement attestation program"

[lib]
crate-type = ["cdylib", "lib"]
name = "renew_protocol"

[features]
default = []
cpi = ["no-entrypoint"]
no-entrypoint = []
no-idl = []
no-log-ix-name = []
idl-build = ["anchor-lang/idl-build"]

[dependencies]
anchor-lang = { workspace = true, features = ["init-if-needed"] }

[dev-dependencies]
serial_test = "3.2.0"
solana-program-test = "2.3"
solana-sdk = "2.3"
solana-system-interface = "1.0.0"
tokio = { version = "1", features = ["macros", "rt-multi-thread"] }
